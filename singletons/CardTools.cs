using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//singlton
public class CardTools: SingletonBase<CardTools>
{

    /*
    public List<int> DetermineActivatableEffects(ICard card){
        List<int> effectIndexes = new List<int>(); 
        for (int i = 0; i < card.effects.Count; i++)
        {
            if(card.effects[i].Conditional()){
                effectIndexes.Add(i);
            }
        }

        return effectIndexes;
    }

    public void ActivateEffectByIndex(List<ICardEffect> effects, int index){
        effects[index].Effect();
    }

    public void DetermineActivatableByIndex(List<ICardEffect> effects, int index){
        effects[index].ActivateConditional();
    }
*/





}