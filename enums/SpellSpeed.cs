public enum SpellSpeed
{
    Speed_1,
    Speed_2,
    Speed_3,
    Speed_4,
    none //happens immediatly, does not leave events, cannot be chained to.. maybe not needed because of the classification of ConstantActivated effect.
}
